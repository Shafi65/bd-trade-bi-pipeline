# Day 0 verification needs only these two.
requests>=2.31.0        # raw HTTP calls, so we SEE the Comtrade API
python-dotenv>=1.0.0    # loads the git-ignored .env so our key never touches code

# --- M1: BBS Excel extraction ---
pandas>=2.0             # reshape (wide->long) + group-by reconciliation
openpyxl>=3.1           # read the .xlsx workbook (streaming, read-only)

# --- Later milestones (uncomment when reached) ---
# pyarrow               # write partitioned Parquet (M3)
# boto3                 # AWS SDK for Python (M4+)
# comtradeapicall       # official UN Comtrade client (stretch scope)
